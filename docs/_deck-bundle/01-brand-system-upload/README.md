# Brand-system upload folder

Populate with optional brand assets (fonts, a `bayan` caliber-reference screenshot, brand-spec.md, design-tokens.css), then upload the whole folder to Claude Design (see ../README.md Step 2). May be left empty — the brand is defined in this step.
