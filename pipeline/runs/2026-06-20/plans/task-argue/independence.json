{"verdict": "single_origin", "origins": ["MiniMax (vendor) -- the MiniMax M3 launch: SWE-Bench Pro score, open-weight release plan, and Sparse Attention specs are all MiniMax-supplied claims"], "reason": "All three load-bearing claims trace to a single origin: MiniMax's own M3 launch. s1 (The Decoder) reports the 59 percent SWE-Bench Pro figure explicitly 'according to MiniMax' -- it relays the vendor's self-reported benchmark, not an independent test. s2 and s3 are the SAME URL (https://www.together.ai/models/minimax-m3); Together AI is a hosting/launch partner restating vendor-supplied facts (the release timeline and the MiniMax Sparse Attention compute and speedup specs are MiniMax's own architecture claims). The Decoder and Together AI are distinct hosts but neither is an independent verifier -- both re-run MiniMax's launch claims, so the sourcing collapses to one origin."}